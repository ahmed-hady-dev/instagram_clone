import 'package:flutter/material.dart';

class SignUpWeb extends StatelessWidget {
  const SignUpWeb({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Center(
          child: Text('SignUpWeb'),
        ),
      ),
    );
  }
}
